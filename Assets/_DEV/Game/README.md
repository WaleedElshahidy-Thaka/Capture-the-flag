# Game

The actual gameplay scene: real player robot controller, flag capture logic, match rules.

Not started yet. Comes after the Matchmaking flow (see `../Matchmaking/`) reaches
`StartMatch` with placeholder shapes proving the flow works, and after the real Photon
Fusion-networked player controller exists (see `../AIBots/` for why that ordering matters).
