# AI Bots

Core bot AI: movement, targeting, difficulty. Fills the seats Matchmaking couldn't fill with
real players (see `../Matchmaking/`'s bot-fill count at `StartMatch`).

Not started yet. Bots are built and tuned against the real player controller once it exists
(`../Game/`), not against placeholders — there's nothing meaningful to react to yet.
